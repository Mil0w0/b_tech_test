services:
  api:
    build: .
    ports:
      - 3002:3000
    volumes:
      - .:/app
    env_file:
      - environment/api.dev.env

  db:
    image: postgres:alpine
    env_file:
      - environment/api.dev.env
